'use client'

import { ReactNode } from "react";

type AoscompoProps = {
  children: ReactNode;
};

const Aoscompo = ({ children }: AoscompoProps) => {
  return <>{children}</>;
};

export default Aoscompo;
